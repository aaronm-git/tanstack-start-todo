# Architecture overview

High-level overview of the app’s architecture and where to make changes.

## Tech stack

- TanStack Start (app framework)
- TanStack Router (routing)
- TanStack Store (state management)
- Tailwind CSS (styling)
- Anthropic (Claude) API (AI integration)

## Code map (starting points)

- Routes: `src/routes/`
- UI components: `src/` (search for `components`)
- Server/integration code: `src/` (search for API handlers and server functions)

